# Sustain 2.0.0 release execution

Release metadata: `v2.0.0`, build `6`, macOS `14.0.0` minimum. Public release
1.1.1 (build 4) contains Sparkle, and the stable appcast exists. A live Mac
test on 2026-09-26 found that 1.1.1 never enables its updater because its
Developer ID certificate check omits `kSecCSSigningInformation`. RC2 contains
the same defect. The production feed must remain unchanged while the bootstrap
recovery and staged lower-to-higher update are resolved.

## Evidence recorded

| Gate | Result | Evidence |
| --- | --- | --- |
| Clean release commit | Pending | The candidate needs the Sparkle eligibility fix, build 6, and a fresh staging run after the 1.1.2 bootstrap. |
| Metadata and prior feed validation | Pending recheck | The public signed appcast currently contains 1.1.1/build 4. Revalidate after 1.1.2/build 5 is public. |
| Immutable tag | Pending | Create `v2.0.0` only after the final release commit is reviewed. |
| Swift tests | Passed locally, 2026-09-25 | `swift test`: 201 tests in 18 suites. Protected workflow repeats this on the tagged commit. |
| Source CI | Passed for `c18e0dc`, 2026-09-25 | [macOS run 36214385664](https://github.com/dillonlui/sustain/actions/runs/36214385664) completed successfully. Rerun on the final release commit. |
| Universal 2 package | Passed locally, ad hoc, 2026-09-25 | `scripts/package.sh debug` verified the arm64/x86_64 app, nested Sparkle signatures, entitlements, DMG checksum, and DMG/ZIP app identity. Official Developer ID verification remains pending. |
| Protected staging path | Exercised through RC2 | `Protected Sparkle rehearsal` runs from a unique RC tag, builds and notarizes a candidate, publishes a prerelease and signed appcast on `staging-updates`, and leaves the production feed alone. The first dispatch from `main` was rejected by the environment's tag-only policy before any step ran. |
| RC1 rehearsal | Failed after asset upload, 2026-09-26 UTC | [Run 36215800574](https://github.com/dillonlui/sustain/actions/runs/36215800574) passed tests, Developer ID signing, app/DMG notarization, container checks, and appcast signing. Its public prerelease assets uploaded, then hash verification failed because the files were downloaded to `/tmp/staging-dist` while the checksum entries named `dist/...`. The final staging-feed step was skipped; at that point `staging-updates` did not exist and production `updates` remained unchanged. |
| RC2 protected rehearsal | Packaging passed; updater blocked | [Run 36252605281](https://github.com/dillonlui/sustain/actions/runs/36252605281) passed tests, Developer ID signing, app and DMG notarization/stapling, recursive container checks, Sparkle appcast signing/verification, public asset hash checks, and staging appcast publication. It used build 5 and contains the eligibility defect. |
| Developer ID and notarization | RC2 passed; corrected candidate pending | App submission `90742dd0-cf98-4ca7-8d40-5b63d79a77cf` and DMG submission `289ec115-26f4-41ba-8987-b6b53f85c66c` were both Accepted. Record new submissions for corrected builds. |
| Staging Sparkle update | Awaiting 1.1.2 bootstrap and new 2.0 RC | On an Apple silicon Mac running macOS 26.6.2, the signed public 1.1.1/build 4 app showed no **Check for Updates…** command. A direct Security API reproduction returned zero certificates with `SecCodeCopySigningInformation(..., [])` and three certificates, including the Developer ID Application leaf, with `kSecCSSigningInformation`. The same missing flag exists in the RC2 source. A corrected 1.1.2/build 5 manual bootstrap and 2.0/build 6 staging update are planned. |
| Manual release QA | Pending | Record machines, macOS versions, peripherals, scenarios, and outcomes below. |
| Production release and appcast | Pending | Record immutable asset URLs, SHA-256 hashes, signed appcast verification, and feed commit. |

## Staging update walkthrough

Record the corrected lower-build → 2.0.0 path, staging appcast URL,
notarization submission IDs, ZIP and DMG SHA-256 hashes, and the Mac and macOS
version used. Install the lower build in `/Applications` and confirm:

Use a disposable macOS test account or a temporary backed-up test library in
the current account. Sparkle 2.9.6 allows the signed lower
bundle's `SUFeedURL` to be overridden through that account's `com.sustain.app`
user defaults, so it can read the staging feed without editing or re-signing the
public app. Verify the override is active before checking, and delete it when
the rehearsal ends. The production feed and everyday user data must stay
untouched during this test.

After the protected rehearsal workflow publishes the staging appcast, run these
commands in the disposable account with Sustain closed:

```sh
defaults write com.sustain.app SUFeedURL \
  'https://raw.githubusercontent.com/dillonlui/sustain/staging-updates/appcast.xml'
defaults read com.sustain.app SUFeedURL
```

After the rehearsal, close Sustain and run
`defaults delete com.sustain.app SUFeedURL`. Confirm the setting is absent
before using that account for normal update checks.

- [ ] Manual check, optional scheduled check, already-current result, release
      notes, Install, Later, Skip, and a still-newer version after Skip.
- [ ] Download, signature verification, installation, relaunch, and preserved
      library, custom pad references, MIDI mappings, and preferences.
- [ ] Checks and relaunch defer during Live playback, idle pad pre-roll,
      transition/fade, and Rehearse playback; deferred work resumes when idle.
- [ ] Offline, DNS, TLS, malformed/tampered feed and archive, and incompatible
      macOS cases fail without replacing the installed app.
- [ ] Read-only DMG, locked destination, canceled authorization, save failure,
      and failed installation provide a recoverable path.

## Final candidate QA

- [ ] Review audio, routing, custom pad import/relink, and MIDI behavior on the
      signed app with representative devices and files.
- [ ] Review Live/Rehearse states, keyboard focus, increased contrast, Reduce
      Motion, and VoiceOver on the signed app. Repeat the song-title/tempo field
      arrow-key check from `docs/21_2_0_UX_UI_Launch_Audit.md`.
- [ ] Verify Apple silicon and Intel hardware, or document approved scope and
      corresponding public note before launch.
- [ ] Verify the public DMG, ZIP, and checksum URLs and hashes. Confirm the
      signed appcast references the immutable ZIP and is published last.

## Release record

Fill this section with the final tag/commit, protected workflow URL, staging
feed URL, test machine details, notarization IDs, artifact hashes, public asset
URLs, feed commit, and any approved exceptions before declaring 2.0 released.

### RC2 staging assets and feed

- Source/ref: `v2.0.0-rc2` at `932abe9d7a93472ec19c7bad28afa7a14d254960`.
- Prerelease: [Sustain v2.0.0-rc2](https://github.com/dillonlui/sustain/releases/tag/v2.0.0-rc2).
- Staging appcast: `https://raw.githubusercontent.com/dillonlui/sustain/staging-updates/appcast.xml`, committed at `9bedd3765319ce0194813fe0aca3424ffebfcb4a`. It passed `scripts/validate-appcast.sh`, names version 2.0.0/build 5, and references the immutable RC2 ZIP. The protected workflow also verified its EdDSA signature.
- Public DMG SHA-256: `0f3228ed51f3a06de019c3d3718c33c5187840b7e0e108b917e0eca99bc4df0f`.
- Public Sparkle ZIP SHA-256: `305309b82f5b275d2eb452caa8594fdc50eb9ad70c4b4f5977e207d019cc3107`.
- The downloaded public checksum file matches GitHub's asset digests, and the protected workflow's independent downloads passed `shasum -c`.
- Production `updates` branch remained at `09c3e4081f1923373f48fa76a985d279bb1fa612`, and the latest stable release remained v1.1.1 after RC2 publication.
